<%- include("partials/start") %>

<section class="container section">
  <div class="empty-box">
    <h1><%= statusCode %></h1>
    <p><%= message %></p>
    <% if (error && process.env.NODE_ENV !== "production") { %>
      <pre><%= error.message %></pre>
    <% } %>
    <a class="btn btn-primary" href="/">Voltar para início</a>
  </div>
</section>

<%- include("partials/end") %>
